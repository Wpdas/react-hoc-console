{
  "name": "react-hoc-console",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.11.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "mkdir assets && cp ./src/assets/* ./assets && cp ./src/reactConsole.js ./ && cp ./src/reactConsole.module.scss ./"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
